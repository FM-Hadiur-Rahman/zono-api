DATABASE_URL="postgresql://postgres:postgres@localhost:5432/zono?schema=public"

JWT_SECRET=zonoisaemployeemanagementappthat
PORT=4000
NODE_ENV=production

# ───── EMAIL (GMAIL SMTP) ─────
EMAIL_ENABLED=true
EMAIL_USER=zonoworkforce@gmail.com
EMAIL_PASS=xucdmurlwnliivjf   # ← this is your Google App Password (OK to keep as-is)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# ───── FRONTEND / CORS ─────
# Use your deployed frontend URL here (Render or Vercel)
APP_ORIGIN=https://zono-web.vercel.app
CORS_ORIGINS=https://zono-web.vercel.app

# ───── MISC ─────
ALLOW_OWNER_SELF_SIGNUP=true