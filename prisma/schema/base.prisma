generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"] // ← only needed if using an older Prisma (<5.15)
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
